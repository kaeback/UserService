spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/userservice
    username: root
    password: 1234
  
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

# 바인딩 파라미터 확인
logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: trace